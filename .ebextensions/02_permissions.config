container_commands:
  01_fix_react_app_rewired_permissions:
    command: "chmod +x /var/app/current/node_modules/.bin/react-app-rewired"
    leader_only: true # It's generally good practice to run this only on one instance if you have multiple.